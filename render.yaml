services:
  - type: web
    name: lego-recommender
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn main:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: ANTHROPIC_API_KEY
        sync: false
      - key: REBRICKABLE_API_KEY
        sync: false
